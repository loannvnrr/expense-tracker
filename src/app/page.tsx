export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>Expense Tracker</h1>
      <p>API en ligne. Le dashboard arrive à une étape suivante.</p>
      <p>
        Endpoint de test : <code>GET /api/health</code>
      </p>
    </main>
  );
}
